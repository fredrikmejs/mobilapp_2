<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobilapp_21">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="s185130 Fredriks Galgeleg"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.WordList"></activity>
        <activity android:name=".ui.ChooseWordList" />
        <activity android:name=".ui.SwitchName" />
        <activity android:name=".ui.LostScreen" />
        <activity android:name=".ui.WonScreen" />
        <activity android:name=".ui.Settings" />
        <activity android:name=".ui.Choose_game" />
        <activity android:name=".ui.GalgeGame" />
        <activity android:name=".ui.Difficulty" />
        <activity android:name=".ui.Welcome_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>